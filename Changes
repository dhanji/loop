TODO
----

Parsing Classes
- class declaration
- enumeration type
- class constants?
x 'where' scoping
- Pattern matching methods
  - match lists, empty, mixed with literals, regexes
  - match strings, regexes
  - match against single literals
  - match character streams
  - match patterns against multiple arguments

- Pattern matching methods on character streams
- guards for pattern matching methods
- Type classes (better than interfaces/traits)
- Interactive interpreter/shell
x Calling into Java methods
- Parsing require decl hiding (stuff)


Stretch
-------

- Tail call optimizer
- Basic function inlining
