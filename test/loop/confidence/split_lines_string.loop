# Pattern Matching split lines in a string

unline(input) =>
  /(?<one>hello)\n(?<two>there)\n(?<three>dude)/  : one + two + three
  *                                                 : ''

main ->
  unline("hello\nthere\ndude")
