def reverse(ls) {
  if (ls is java.util.List) {
    if (ls == empty) {
     return ([]);
    }
  ;}
  if (ls is java.util.List) {
    x = ls[0];
    xs = ls.subList(1,ls.size() - 1);
    return (reverse((xs))+ x);
  }
  ;}

}

def main() {
reverse([1, 2, 3])
}

